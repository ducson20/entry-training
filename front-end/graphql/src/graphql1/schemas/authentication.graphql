type LoginResponse {
  status: String
  UserGUID: String
}

type Mutation {
  login(input: LoginInput!): LoginResponse
}

input LoginInput {
  username: String!
  password: String!
  website: String!
  loginmode: String!
}
